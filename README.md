# Inventory Management System

## 📖 Оглавление
- [Общее описание](#-общее-описание)
- [Архитектура системы](#-архитектура-системы)
- [Особенности реализации](#-особенности-реализации)
- [Установка и запуск](#-установка-и-запуск)
- [Руководство пользователя](#-руководство-пользователя)
- [Технические детали](#-технические-детали)
- [Структура проекта](#-структура-проекта)

## 🏢 Общее описание

**Inventory Management System** - это полнофункциональная система управления товарными запасами, разработанная на Java с использованием Swing для графического интерфейса. Система предоставляет комплексное решение для учета товаров, включая базовые операции CRUD, расширенный мониторинг и SQL-подобный интерфейс для работы с данными.

### Ключевые возможности:
- 🔐 **Безопасное хранение** данных с AES-шифрованием
- 📊 **Реал-тайм мониторинг** статистики и метрик
- 🗃️ **SQL-консоль** для продвинутой работы с данными
- 📝 **Детальное логирование** всех операций
- 💾 **Автоматическое резервное копирование**
- 🎯 **Гибкая система поиска** и фильтрации

## 🏗️ Архитектура системы

### Модель данных (Model Layer)
```java
// Основная сущность - товарная запись
public class Record {
    public int id;           // Уникальный идентификатор
    public String name;      // Наименование товара
    public int quantity;     // Количество на складе
    public double price;     // Цена за единицу
    public String supplier;  // Поставщик
}
```

### Компоненты системы:
1. **Database** - ядро системы, отвечает за хранение и обработку данных
2. **MainWindow** - главное окно с табличным представлением данных
3. **Monitoring** - панель мониторинга с аналитикой
4. **SQLConsolePanel** - консоль для выполнения SQL-подобных запросов

## ⭐ Особенности реализации

### 🔐 Система шифрования
**Уровень безопасности:** Высокий (AES-256)

```java
// Все данные шифруются перед сохранением на диск
private byte[] encrypt(byte[] data) throws GeneralSecurityException {
    Cipher cipher = Cipher.getInstance("AES");
    cipher.init(Cipher.ENCRYPT_MODE, secretKey);
    return cipher.doFinal(data);
}
```

**Особенности:**
- Автоматическое шифрование/дешифрование при загрузке/сохранении
- Использование стандартного AES алгоритма
- Ключ шифрования хранится в коде (для демонстрационных целей)

### 📊 Система мониторинга
**Панель Monitoring предоставляет:**

**Основная статистика:**
- Количество уникальных товаров
- Общее количество единиц товара
- Общая стоимость запасов
- Товары с низким остатком (<5 единиц)

**Топ-аналитика:**
- Топ-5 поставщиков по стоимости
- Топ-5 поставщиков по количеству товаров
- Самые дорогие товары
- Товары с наибольшим количеством

**Особенности:**
- Автоматическое обновление при изменении данных
- Observer pattern для real-time обновлений
- Визуализация в HTML-формате

### 🗃️ SQL-подобный интерфейс
**Поддерживаемые команды:**

```sql
-- Выборка данных
SELECT *                          -- Все записи
SELECT * WHERE price > 1000       -- Фильтрация по условию

-- Управление данными
INSERT id=1 name="TV" quantity=10 price=500 supplier="Sony"
UPDATE SET price=900 WHERE name="TV"
DELETE * WHERE quantity < 5
DELETE *                          -- Очистка всей базы

-- Справка
HELP
```

**Особенности синтаксиса:**
- Поддержка операторов сравнения: `=, >, <, >=, <=`
- Строковые значения в двойных кавычках
- Автоматическое определение типа данных

### 📝 Система логирования
**Логирование всех операций:**
```
[Mon Dec 11 14:30:25 MSK 2023] ADD: 1;TV;10;500.0;Sony
[Mon Dec 11 14:31:10 MSK 2023] SELL: id=1 amount=2
[Mon Dec 11 14:32:15 MSK 2023] SEARCH name=TV
```

**Особенности:**
- Автоматическое логирование в файл `operations.log`
- Временные метки для каждой операции
- Подробное описание выполненных действий

## 🚀 Установка и запуск

### Требования:
- **Java JDK 17** или выше
- **Maven** (для сборки зависимостей)

### Запуск приложения:
```bash
# Клонирование репозитория
git clone https://github.com/your-username/inventory-management-system.git
cd inventory-management-system

# Компиляция и запуск
javac -cp "."
src/model/*.java src/gui/*.java
java -cp "src" gui.MainWindow
```

### Структура файлов:
```
products.db          # Основная база данных (зашифрованная)
products.db.bak      # Автоматическая резервная копия
products_backup.db   # Ручная резервная копия
operations.log       # Журнал операций
```

## 👨‍💻 Руководство пользователя

### Основные операции:

#### 1. Добавление товара
- Нажмите кнопку "Добавить"
- Заполните все поля: ID, Название, Количество, Цена, Поставщик
- Система автоматически проверит уникальность ID

#### 2. Управление запасами
- **Поставка**: Увеличение количества товара
- **Продажа**: Уменьшение количества (проверка достаточности)
- **Удаление**: Полное удаление товара по ID

#### 3. Поиск и фильтрация
- Простой поиск по любому полю
- Поддержка поиска по точному совпадению
- Результаты отображаются в основной таблице

#### 4. Резервное копирование
- **Backup**: Создание резервной копии вручную
- **Restore**: Восстановление из резервной копии
- **Auto-Backup**: Автоматическое создание бэкапа при загрузке

### Работа с SQL-консолью:

#### Базовые примеры запросов:

```sql
-- Просмотр всех товаров
SELECT *

-- Поиск дорогих товаров
SELECT * WHERE price > 1000

-- Поиск товаров от конкретного поставщика
SELECT * WHERE supplier="Sony"

-- Добавление нового товара
INSERT id=100 name="Ноутбук" quantity=5 price=1500 supplier="Dell"

-- Обновление цены
UPDATE SET price=1200 WHERE name="Ноутбук"

-- Удаление товаров с низким запасом
DELETE * WHERE quantity < 3
```

## 🔧 Технические детали

### Паттерны проектирования:

#### 1. Observer Pattern
```java
// Система уведомлений об изменениях в БД
public interface DatabaseListener {
    void onDatabaseChanged();
}

// Автоматическое обновление UI при изменениях
@Override
public void onDatabaseChanged() {
    refreshTable();
    monitoring.refresh();
}
```

#### 2. Indexing System
```java
// Индексы для быстрого поиска
private Map<String, Set<Integer>> nameIndex = new HashMap<>();
private Map<String, Set<Integer>> supplierIndex = new HashMap<>();
```

### Алгоритмы и структуры данных:

#### Поиск с индексацией:
- **O(1)** для поиска по имени и поставщику
- **O(n)** для поиска по цене и количеству
- Поддержка сложных условий через stream API

#### Сортировка:
- Поддержка сортировки по всем полям
- Использование Comparator для гибкой сортировки

### Обработка ошибок:
- Валидация данных при добавлении/обновлении
- Защита от дублирования ID
- Проверка достаточности товара при продаже
- Обработка ошибок ввода пользователя

## 📁 Структура проекта

```
src/
├── model/
│   ├── Database.java          # Ядро системы управления данными
│   └── Record.java           # Модель товарной записи
├── gui/
│   ├── MainWindow.java       # Главное окно приложения
│   ├── Monitoring.java       # Панель мониторинга и аналитики
│   └── SQLConsolePanel.java  # SQL-консоль для запросов
└── Main.java                # Точка входа в приложение
```

### Модель данных (Database.java):
- **Шифрование/дешифрование** данных
- **Управление индексами** для быстрого поиска
- **Логирование** операций
- **Резервное копирование**
- **Observer pattern** для уведомлений

### Пользовательский интерфейс:
- **MainWindow**: Табличное представление, основные операции
- **Monitoring**: Визуализация статистики и аналитики
- **SQLConsolePanel**: Расширенный интерфейс для работы с данными
